import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
import vm from 'node:vm';
import ts from 'typescript';
import {DatabaseSync} from 'node:sqlite';
function load(file,deps={},mode='development'){
 const exports={};const code=ts.transpileModule(fs.readFileSync(new URL(`../${file}`,import.meta.url),'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText;
 vm.runInNewContext(code,{exports,URL,Response,TextDecoder,Uint8Array,Date,process:{env:{NODE_ENV:mode}},require(name){if(name in deps)return deps[name];if(name==='next/server')return {NextResponse:Response};if(name.startsWith('@/'))return load(`${name.slice(2)}.ts`,deps,mode);throw Error(name);}});return exports;
}
const {validateCollectionResult:validate}=load('app/collection-result.ts');
const sample=()=>({schemaVersion:1,sourceUrl:'https://detail.1688.com/offer/123.html?tracking=x',provider:'fixture-provider',collectedAt:'2026-01-01T00:00:00Z',title:'原文商品',description:'原文',options:[{sku:'sku-1',name:'黑色',unitPriceCny:3.25,minimumOrder:2,stock:null}],images:[{url:'https://cbu01.alicdn.com/img/test.jpg',role:'main'}]});
test('collection validates observed source fields without inventing translation or stock',()=>{const r=validate(sample(),'123');assert.equal(r.sourceUrl,'https://detail.1688.com/offer/123.html');assert.equal(r.title,'原文商品');assert.equal(r.options[0].stock,null);assert.equal(r.options[0].unitPriceCny,3.25);});
test('wrong offer, unknown fields, future timestamps and invalid prices rejected',()=>{assert.throws(()=>validate(sample(),'456'));for(const patch of [{cookie:'secret'},{collectedAt:'2999-01-01T00:00:00Z'},{options:[]},{options:[{...sample().options[0],unitPriceCny:0}]},{options:[{...sample().options[0],stock:-1}]}])assert.throws(()=>validate({...sample(),...patch},'123'));});
test('duplicate SKU and unsafe or duplicate images rejected',()=>{assert.throws(()=>validate({...sample(),options:[...sample().options,...sample().options]},'123'));for(const url of ['http://cbu01.alicdn.com/a','https://127.0.0.1/a','https://alicdn.com.evil.test/a','https://user:pass@cbu01.alicdn.com/a'])assert.throws(()=>validate({...sample(),images:[{url,role:'main'}]},'123'));assert.throws(()=>validate({...sample(),images:[...sample().images,...sample().images]},'123'));});
function store(){const sqlite=new DatabaseSync(':memory:');sqlite.exec("CREATE TABLE collection_jobs(id TEXT PRIMARY KEY,owner_id TEXT,status TEXT); INSERT INTO collection_jobs VALUES('j','owner','awaiting_connector'),('c','owner','cancelled')");const db={prepare(sql){let args=[];const q={bind(...values){args=values;return q;},async run(){return sqlite.prepare(sql).run(...args);},async first(){return sqlite.prepare(sql).get(...args)??null;}};return q;}};return {sqlite,...load('db/collection-results.ts',{'cloudflare:workers':{env:{DB:db}}})};}
test('SQLite receipt retries are idempotent and preserve first result',async()=>{const s=store(),r=validate(sample(),'123');assert.equal((await s.storeCollectionResult('owner','j',r)).status,'stored');assert.equal((await s.storeCollectionResult('owner','j',r)).status,'stored');assert.equal((await s.storeCollectionResult('owner','j',{...r,title:'changed'})).status,'conflict');assert.equal((await s.readCollectionResult('owner','j')).result.title,r.title);assert.equal(s.sqlite.prepare('SELECT count(*) AS n FROM collection_results').get().n,1);s.sqlite.close();});
test('SQLite cancelled and foreign owner requests cannot write or read',async()=>{const s=store(),r=validate(sample(),'123');assert.equal((await s.storeCollectionResult('other','j',r)).status,'cancelled');assert.equal((await s.storeCollectionResult('owner','c',r)).status,'cancelled');await s.storeCollectionResult('owner','j',r);assert.equal(await s.readCollectionResult('other','j'),null);s.sqlite.close();});
function route(mode='development',overrides={}){let writes=0;const api=load('app/api/collection-jobs/[id]/result/route.ts',{'@/app/chatgpt-auth':{getChatGPTUser:async()=>null,getWorkspaceOwnerId:async()=>'owner'},'@/db/collection-jobs':{findCollectionJob:async()=>({offer_id:'123',status:'awaiting_connector'})},'@/db/collection-results':{readCollectionResult:async()=>null,storeCollectionResult:async(o,id,result)=>{writes++;return {status:'stored',result,receivedAt:'now'};}},...overrides},mode);return {...api,writes:()=>writes};}
const ctx={params:Promise.resolve({id:'j'})};const request=body=>new Request('http://localhost/api/result',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});
test('API requires verified production access and never creates product on receipt',async()=>{const closed=route('production');assert.equal((await closed.POST(request(sample()),ctx)).status,503);assert.equal(closed.writes(),0);const api=route();const response=await api.POST(request(sample()),ctx);assert.equal(response.status,200);const body=await response.json();assert.equal(body.productCreated,false);assert.equal(body.executionStarted,false);assert.equal(api.writes(),1);});
test('API rejects oversized bodies and invalid offers before storage',async()=>{const api=route();assert.equal((await api.POST(request({...sample(),description:'x'.repeat(524289)}),ctx)).status,413);assert.equal((await api.POST(request({...sample(),sourceUrl:'https://detail.1688.com/offer/999.html'}),ctx)).status,400);assert.equal(api.writes(),0);});
