# 양치용품정리 64497 견적 대조 — 2026-09-24

## 확인

Chrome의 로그인된 쿠플러스 AI상품등록에서 7일 조회 후 기존 상품 260921009001의 화이트 옵션 견적을 열었다. 저장·AI 처리·등록 전송은 실행하지 않았다.

- 분류: 생활용품 > 욕실용품 > 욕실수납/정리 > 양치용품정리 (64497).
- 노출 속성 2개, 비노출 속성 33개, 상품고시 5개. 표시된 선택 목록은 함께 저장한 JSON에 기록했다.
- 모든 작업 열이 완료인 상품에 등록실패와 `"상품정보제공고시" - '상품정보제공고시'를 모두 입력해주세요`가 표시됐다.
- 확인한 화이트 옵션의 인증/허가 사항은 공란이고 다른 네 고시에는 값이 있었다. 모델명은 별표가 없고 KCS 인증번호 필드가 있다.
- Supplier Hub 대량등록 화면은 로그인 상태이며 견적서·이미지·필수 표시사항·서류 업로드가 별도이다.

## 추정과 구현 판단

인증/허가 사항 공란이 등록 실패와 관련됐을 가능성이 있다. 실제 전송 요청/서버 응답과 다른 세 옵션을 확인하지 않았으므로 단일 원인으로 확정하지 않는다. 앱에서는 이 분류의 다섯 고시를 미입력 검토 대상으로 지정한다. 이 조건은 관찰된 실패 메시지에 따른 사전 검사이며 공식 필드 스키마를 확보했다는 의미는 아니다.

## 미확인

- 기존 상품의 저장값이 최초 자동 기본값인지, AI 결과인지, 사용자의 수정값인지 구분되지 않는다. 따라서 포장 100g/100*100*100, 해당사항없음, 원산지, 브랜드 등의 저장값을 새 상품 기본값으로 복사하지 않았다.
- 쿠플러스 화면의 선택 문구는 로컬 폼 값으로 보존했지만 Supplier Hub 내부 선택 코드나 빈 값 인코딩은 미확인이다.
- Supplier Hub 80719 양식 탐색 시 상품명 입력이 다음 이동 후 비어 기본정보에서 진행하지 못했다. 이 세션에서는 이미지·법적·물류 단계 신규 확인을 완료하지 못했다. 저장/등록은 실행하지 않았다.
- 64497 공식 Excel/Hub 동일 분류의 입력 규격, 실제 상품 수집·번역·전송은 미검증이다.

## 반영

카테고리 선택/검색, 분류 전용 속성·고시, 기존 콘텐츠/옵션/가격/이미지의 공통 resolver, 수동값 우선순위, 실제 열 이름에 의한 Excel 매핑 경로를 연결했다. tooth_ 필드는 다른 카테고리 프로필에 저장할 수 없다. 상세 근거는 docs/couplus-64497-quotation-2026-09-24.json.
